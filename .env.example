# DeepSeek Harness 环境变量模板 —— 复制本文件为 `.env` 并填写真实值。
# `.env` 已被 .gitignore 忽略，切勿把含真实密钥的 `.env` 提交进版本库。

# ── Agent 模型（提交提示、获取回复所需；不填则无法与 agent 对话）──
DEEPSEEK_API_KEY=
# 可选：自定义 DeepSeek API 地址（默认官方）
DEEPSEEK_BASE_URL=

# ── Voice-Context 语音转文字（SiliconFlow 的 FunAudioLLM/SenseVoiceSmall）──
# 也可在 Web 设置页「语音输入 / Voice input」直接填写（写入选定凭据引用）
SILICONFLOW_API_KEY=
